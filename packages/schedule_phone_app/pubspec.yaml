name: schedule_phone_app
description: ...
version: 0.0.2
publish_to: none

resolution: workspace

flutter:
  uses-material-design: true

environment:
  flutter: ^3.38.5
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.4
  watch_connectivity: ^0.2.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  test: ^1.26.2
