name: schedule_phone
description: ...
version: 0.0.2
publish_to: none

resolution: workspace

flutter:
  uses-material-design: true

environment:
  flutter: ^3.38.5
  sdk: ^3.13.3

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.4
  url_launcher: ^6.3.2
  watch_connectivity: ^0.2.10

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  test: ^1.26.2
